#include "overlay02_safari_zone_to42_private.h"

int ov02_0224EF6C(void *fieldSystem, u32 behavior, u32 which) {
    if (which == 0) {
        return ov02_0224E4CC(fieldSystem, behavior);
    }
    return ov02_0224E4DC(fieldSystem, behavior);
}
