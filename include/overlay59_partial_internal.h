#ifndef OVERLAY_59_PRIVATE_H
#define OVERLAY_59_PRIVATE_H

#include "global.h"

/* The exact subset uses byte-addressed private state because the complete
 * overlay state layout is not yet named. Offsets are preserved in candidate.c. */

#endif
